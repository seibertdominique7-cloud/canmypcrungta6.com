/* eslint-disable @next/next/no-img-element -- CMS media hosts are configured at runtime. */
import type { ArticleRecord } from '../../lib/cms-types';
export function ContentCard({ article }: { article: ArticleRecord }) { const category = article.categories.find((item) => item.isPrimary) ?? article.categories[0]; return <article className="flex h-full flex-col overflow-hidden rounded-3xl border border-white/10 bg-white/[0.04]"><a aria-label={`Read ${article.title}`} href={`/articles/${article.slug}`}>{article.featuredImage ? <img alt={`${article.title} featured image`} className="aspect-[16/9] w-full bg-black/20 object-cover" loading="lazy" src={article.featuredImage} /> : <div aria-hidden="true" className="aspect-[16/9] bg-gradient-to-br from-violet-500/20 to-cyan-500/10" />}</a><div className="flex flex-1 flex-col p-5">{category ? <a className="text-xs font-black uppercase tracking-wider text-violet-300" href={`/category/${category.slug}`}>{category.name}</a> : null}<h2 className="mt-2 text-xl font-black text-white"><a href={`/articles/${article.slug}`}>{article.title}</a></h2><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-400">{article.excerpt}</p><p className="mt-auto pt-4 text-xs text-slate-500">{formatDate(article.publishedAt ?? article.scheduledAt ?? article.createdAt)}</p></div></article>; }
function formatDate(value: string) { return new Intl.DateTimeFormat('en-US', { dateStyle: 'medium' }).format(new Date(value)); }
