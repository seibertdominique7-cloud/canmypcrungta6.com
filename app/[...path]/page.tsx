import { notFound, redirect } from 'next/navigation'; import { findRedirect } from '../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page({ params }: { params: Promise<{ path: string[] }> }) { const source = `/${(await params).path.join('/')}`; const route = await findRedirect(source); if (route) redirect(route.destinationPath); notFound(); }
