import type { Metadata } from 'next'; import { notFound, redirect } from 'next/navigation'; import { PublicArticle } from '../../components/content/PublicArticle'; import { findRedirect, getPublishedArticleBySlug } from '../../lib/cms-data'; import { contentMetadata } from '../../lib/seo'; export const dynamic = 'force-dynamic';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const article = await getPublishedArticleBySlug(slug); return article ? contentMetadata(article, `/articles/${slug}`) : { title: 'Article not found', robots: { index: false } }; }
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = await getPublishedArticleBySlug(slug); if (!article) { const route = await findRedirect(`/articles/${slug}`); if (route) redirect(route.destinationPath); notFound(); } return <PublicArticle article={article} />; }
