import { getNewsArticles } from '../lib/cms-data'; import { getSiteUrl } from '../lib/seo'; export const dynamic = 'force-dynamic'; export async function GET() { const site = getSiteUrl(); const articles = await getNewsArticles(); const xml = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:news="http://www.google.com/schemas/sitemap-news/0.9">${articles.map((item) => `<url><loc>${site}/articles/${encodeURIComponent(item.slug)}</loc><news:news><news:publication><news:name>CanMyPCRunGTA6</news:name><news:language>en</news:language></news:publication><news:publication_date>${(item.publishedAt ?? item.scheduledAt ?? new Date()).toISOString()}</news:publication_date><news:title>${escapeXml(item.title)}</news:title></news:news></url>`).join('')}</urlset>`; return new Response(xml, { headers: { 'Content-Type': 'application/xml; charset=utf-8', 'Cache-Control': 'public, max-age=300' } }); } function escapeXml(value: string) { return value.replace(/[<>&'\"]/g, (character) => ({ '<': '&lt;', '>': '&gt;', '&': '&amp;', "'": '&apos;', '"': '&quot;' })[character] ?? character); }
