import { getRecentArticles } from '../lib/cms-data'; import { getSiteUrl } from '../lib/seo'; export const dynamic = 'force-dynamic'; export async function GET() { const site = getSiteUrl(); const articles = await getRecentArticles(50); const xml = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>CanMyPCRunGTA6 Articles</title><link>${escapeXml(site)}/articles</link><description>GTA VI PC news, requirements, and hardware guides.</description>${articles.map((item) => `<item><title>${escapeXml(item.title)}</title><link>${site}/articles/${encodeURIComponent(item.slug)}</link><guid>${site}/articles/${encodeURIComponent(item.slug)}</guid><description>${escapeXml(item.excerpt)}</description><pubDate>${new Date(item.publishedAt ?? item.scheduledAt ?? item.createdAt).toUTCString()}</pubDate></item>`).join('')}</channel></rss>`; return new Response(xml, { headers: { 'Content-Type': 'application/rss+xml; charset=utf-8', 'Cache-Control': 'public, max-age=300' } }); } function escapeXml(value: string) { return value.replace(/[<>&'\"]/g, (character) => ({ '<': '&lt;', '>': '&gt;', '&': '&amp;', "'": '&apos;', '"': '&quot;' })[character] ?? character); }
