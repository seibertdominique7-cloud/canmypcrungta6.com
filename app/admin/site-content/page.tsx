import { SiteContentAdminDashboard } from '../../components/admin/SiteContentAdminDashboard'; import { requireAdminPage } from '../../lib/admin-auth'; import { getSiteContent } from '../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page() { await requireAdminPage(); return <SiteContentAdminDashboard initialItems={await getSiteContent()} />; }
