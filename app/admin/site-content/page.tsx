import { SiteContentAdminDashboard } from '../../components/admin/SiteContentAdminDashboard'; import { requireAdminPage } from '../../lib/admin-auth'; import { getMediaAssets, getMediaFolders, getSiteContent } from '../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page() { await requireAdminPage(); const [items, media, folders] = await Promise.all([getSiteContent(), getMediaAssets(), getMediaFolders()]); return <SiteContentAdminDashboard initialItems={items} initialMedia={media} initialMediaFolders={folders} />; }
