import { MediaAdminDashboard } from '../../components/admin/MediaAdminDashboard'; import { requireAdminPage } from '../../lib/admin-auth'; import { getMediaAssets, getMediaFolders } from '../../lib/cms-data'; import { getMediaStorageStatus } from '../../lib/media-storage'; export const dynamic = 'force-dynamic'; export default async function Page() { await requireAdminPage(); const [items, folders] = await Promise.all([getMediaAssets(), getMediaFolders()]); return <MediaAdminDashboard initialFolders={folders} initialItems={items} storageStatus={getMediaStorageStatus()} />; }
