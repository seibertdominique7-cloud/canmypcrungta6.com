import { notFound } from 'next/navigation'; import { PublicArticle } from '../../../../components/content/PublicArticle'; import { requireAdminPage } from '../../../../lib/admin-auth'; import { getArticlePreview } from '../../../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page({ params }: { params: Promise<{ id: string }> }) { await requireAdminPage(); const article = await getArticlePreview((await params).id); if (!article) notFound(); return <PublicArticle article={article} preview />; }
