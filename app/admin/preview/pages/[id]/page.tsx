import { notFound } from 'next/navigation'; import { PublicPage } from '../../../../components/content/PublicPage'; import { requireAdminPage } from '../../../../lib/admin-auth'; import { getPagePreview } from '../../../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page({ params }: { params: Promise<{ id: string }> }) { await requireAdminPage(); const page = await getPagePreview((await params).id); if (!page) notFound(); return <PublicPage page={page} preview />; }
