import { TaxonomyAdminDashboard } from '../../components/admin/TaxonomyAdminDashboard'; import { requireAdminPage } from '../../lib/admin-auth'; import { getCategories, getMediaAssets, getMediaFolders } from '../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page() { await requireAdminPage(); const [items, media, folders] = await Promise.all([getCategories(), getMediaAssets(), getMediaFolders()]); return <TaxonomyAdminDashboard initialItems={items} initialMedia={media} initialMediaFolders={folders} kind="category" />; }
