import { TaxonomyAdminDashboard } from '../../components/admin/TaxonomyAdminDashboard'; import { requireAdminPage } from '../../lib/admin-auth'; import { getCategories } from '../../lib/cms-data'; export const dynamic = 'force-dynamic'; export default async function Page() { await requireAdminPage(); return <TaxonomyAdminDashboard kind="category" initialItems={await getCategories()} />; }
