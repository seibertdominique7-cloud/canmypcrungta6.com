import type { Metadata } from 'next'; import { notFound, redirect } from 'next/navigation'; import { PublicPage } from '../../components/content/PublicPage'; import { findRedirect, getPublishedPageBySlug } from '../../lib/cms-data'; import { contentMetadata } from '../../lib/seo'; export const dynamic = 'force-dynamic';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const page = await getPublishedPageBySlug(slug); return page ? contentMetadata(page, page.requiredPageKey ? `/${page.requiredPageKey}` : `/pages/${slug}`) : { title: 'Page not found', robots: { index: false } }; }
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const page = await getPublishedPageBySlug(slug); if (!page) { const route = await findRedirect(`/pages/${slug}`); if (route) redirect(route.destinationPath); notFound(); } if (page.requiredPageKey) redirect(`/${page.requiredPageKey}`); return <PublicPage page={page} />; }
