import { requireAdminApi } from '../../../../../lib/admin-auth';
import { adminRouteError, readJson } from '../../../../../lib/admin-api';
import { deletePage, duplicatePage, getContentWorkspace, moveNavigationPage, restoreRevision, updatePage } from '../../../../../lib/cms-data';
import { validatePageInput } from '../../../../../lib/cms-validation';
import { sanitizeRichTextBody } from '../../../../../lib/rich-text';

export async function PATCH(request: Request, context: RouteContext<'/api/admin/cms/pages/[id]'>) { const denied = await requireAdminApi(request); if (denied) return denied; const { id } = await context.params; const body = await readJson(request); const record = typeof body === 'object' && body !== null ? body as Record<string, unknown> : {}; try { if (record.action === 'duplicate') { await duplicatePage(id); return Response.json({ message: 'Draft copy created.', workspace: await getContentWorkspace() }); } if (record.action === 'restore' && typeof record.revisionId === 'string') { await restoreRevision('page', id, record.revisionId); return Response.json({ message: 'Revision restored.', workspace: await getContentWorkspace() }); } if (record.action === 'move' && (record.direction === 'up' || record.direction === 'down')) { await moveNavigationPage(id, record.direction); return Response.json({ message: 'Navigation order updated.', workspace: await getContentWorkspace() }); } const validation = validatePageInput(body); if (!validation.data) return Response.json({ error: 'Correct the highlighted page fields.', fieldErrors: validation.fieldErrors }, { status: 400 }); await updatePage(id, { ...validation.data, body: sanitizeRichTextBody(validation.data.body) }); return Response.json({ message: 'Page saved.', warnings: validation.warnings, workspace: await getContentWorkspace() }); } catch (error) { return adminRouteError(error); } }
export async function DELETE(request: Request, context: RouteContext<'/api/admin/cms/pages/[id]'>) { const denied = await requireAdminApi(request); if (denied) return denied; try { await deletePage((await context.params).id); return Response.json({ message: 'Page deleted.', workspace: await getContentWorkspace() }); } catch (error) { return adminRouteError(error); } }
